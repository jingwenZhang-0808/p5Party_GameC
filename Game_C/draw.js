function drawText(textName) {
  push();
  fill(255);
  if(page == "text_page0") {
    for(let i = 0; i < shared.textNumber; i ++ ) {
      text(textName[i], 220, 250 + i * text_dist, 500, 30);
    }
  }
  if(page == "text_page1") {
    for(let i = 0; i < shared.textNumber - 3; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page2_1") {
    for(let i = 0; i < shared.textNumber - 4; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page2_2") {
    for(let i = 0; i < shared.textNumber - 6; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page2_3") {
    for(let i = 0; i < shared.textNumber - 8; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page2_4") {
    for(let i = 0; i < shared.textNumber - 10; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page2_5") {
    for(let i = 0; i < shared.textNumber - 12; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page2_6") {
    for(let i = 0; i < shared.textNumber - 14; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page2_7") {
    for(let i = 0; i < shared.textNumber - 16; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page2_8") {
    for(let i = 0; i < shared.textNumber - 18; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page2_9") {
    for(let i = 0; i < shared.textNumber - 20; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page3") {
    for(let i = 0; i < shared.textNumber - 24; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page4") {
    for(let i = 0; i < shared.textNumber - 26; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page5") {
    for(let i = 0; i < shared.textNumber - 29; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page6") {
    for(let i = 0; i < shared.textNumber - 30; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
   else if(page == "text_page7") {
    for(let i = 0; i < shared.textNumber - 32; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page8") {
    for(let i = 0; i < shared.textNumber - 34; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page9") {
    for(let i = 0; i < shared.textNumber - 37; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page10") {
    for(let i = 0; i < shared.textNumber - 40; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page163") {
    for(let i = 0; i < shared.textNumber - 44; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 30);
    }
  }
  else if(page == "text_page11") {
    for(let i = 0; i < shared.textNumber - 36; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page12") {
    for(let i = 0; i < shared.textNumber - 37; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page13") {
    for(let i = 0; i < shared.textNumber - 39; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page14") {
    for(let i = 0; i < shared.textNumber - 41; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page15") {
    for(let i = 0; i < shared.textNumber - 44; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page16") {
    for(let i = 0; i < shared.textNumber - 46; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page17") {
    for(let i = 0; i < shared.textNumber - 44; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page18") {
    for(let i = 0; i < shared.textNumber - 46; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page19") {
    for(let i = 0; i < shared.textNumber - 44; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }  
  else if(page == "text_page20") {
    for(let i = 0; i < shared.textNumber - 46; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page21") {
    for(let i = 0; i < shared.textNumber - 49; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page22") {
    for(let i = 0; i < shared.textNumber - 50; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page23") {
    for(let i = 0; i < shared.textNumber - 52; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page24") {
    for(let i = 0; i < shared.textNumber - 54; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page25") {
    for(let i = 0; i < shared.textNumber - 56; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page26") {
    for(let i = 0; i < shared.textNumber - 58; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page27") {
    for(let i = 0; i < shared.textNumber - 61; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page28") {
    for(let i = 0; i < shared.textNumber - 62; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page29") {
    for(let i = 0; i < shared.textNumber - 64; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page30") {
    for(let i = 0; i < shared.textNumber - 66; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page31") {
    for(let i = 0; i < shared.textNumber - 66; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page32") {
    for(let i = 0; i < shared.textNumber - 67; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page33") {
    for(let i = 0; i < shared.textNumber - 69; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page34") {
    for(let i = 0; i < shared.textNumber - 71; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page35") {
    for(let i = 0; i < shared.textNumber - 73; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page36") {
    for(let i = 0; i < shared.textNumber - 76; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page37") {
    for(let i = 0; i < shared.textNumber - 78; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page38") {
    for(let i = 0; i < shared.textNumber - 80; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page39") {
    for(let i = 0; i < shared.textNumber - 82; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page40") {
    for(let i = 0; i < shared.textNumber - 84; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page41") {
    for(let i = 0; i < shared.textNumber - 86; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page42") {
    for(let i = 0; i < shared.textNumber - 88; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page43") {
    for(let i = 0; i < shared.textNumber - 90; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page44") {
    for(let i = 0; i < shared.textNumber - 92; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page45") {
    for(let i = 0; i < shared.textNumber - 94; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page46") {
    for(let i = 0; i < shared.textNumber - 96; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page47") {
    for(let i = 0; i < shared.textNumber - 98; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page48") {
    for(let i = 0; i < shared.textNumber - 100; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page49") {
    for(let i = 0; i < shared.textNumber - 102; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page50") {
    for(let i = 0; i < shared.textNumber - 104; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page51") {
    for(let i = 0; i < shared.textNumber - 61; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page52") {
    for(let i = 0; i < shared.textNumber - 63; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page53") {
    for(let i = 0; i < shared.textNumber - 65; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page54") {
    for(let i = 0; i < shared.textNumber - 67; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page55") {
    for(let i = 0; i < shared.textNumber - 69; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page56") {
    for(let i = 0; i < shared.textNumber - 71; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page57") {
    for(let i = 0; i < shared.textNumber - 73; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page58") {
    for(let i = 0; i < shared.textNumber - 75; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page59") {
    for(let i = 0; i < shared.textNumber - 77; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page60") {
    for(let i = 0; i < shared.textNumber - 79; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page61") {
    for(let i = 0; i < shared.textNumber - 81; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page62") {
    for(let i = 0; i < shared.textNumber - 83; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page63") {
    for(let i = 0; i < shared.textNumber - 86; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page64") {
    for(let i = 0; i < shared.textNumber - 88; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page65") {
    for(let i = 0; i < shared.textNumber - 90; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page66") {
    for(let i = 0; i < shared.textNumber - 92; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page67") {
    for(let i = 0; i < shared.textNumber - 94; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page68") {
    for(let i = 0; i < shared.textNumber - 96; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page69") {
    for(let i = 0; i < shared.textNumber - 98; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page70") {
    for(let i = 0; i < shared.textNumber - 100; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page71") {
    for(let i = 0; i < shared.textNumber - 86; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page72") {
    for(let i = 0; i < shared.textNumber - 88; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page73") {
    for(let i = 0; i < shared.textNumber - 102; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page74") {
    for(let i = 0; i < shared.textNumber - 104; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page75") {
    for(let i = 0; i < shared.textNumber - 107; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page76") {
    for(let i = 0; i < shared.textNumber - 109; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page164") {
    for(let i = 0; i < shared.textNumber - 113; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page77") {
    for(let i = 0; i < shared.textNumber - 113; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page78") {
    for(let i = 0; i < shared.textNumber - 115; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page79") {
    for(let i = 0; i < shared.textNumber - 112; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page80") {
    for(let i = 0; i < shared.textNumber - 114; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page81") {
    for(let i = 0; i < shared.textNumber - 116; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page82") {
    for(let i = 0; i < shared.textNumber - 118; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page83") {
    for(let i = 0; i < shared.textNumber - 120; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page84") {
    for(let i = 0; i < shared.textNumber - 122; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page85") {
    for(let i = 0; i < shared.textNumber - 124; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page86") {
    for(let i = 0; i < shared.textNumber - 126; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page87") {
    for(let i = 0; i < shared.finalNumber; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page88") {
    for(let i = 0; i < shared.finalNumber - 1; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page89") {
    for(let i = 0; i < shared.finalNumber - 3; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page90") {
    for(let i = 0; i < shared.finalNumber - 5; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page91") {
    for(let i = 0; i < shared.finalNumber - 7; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page92") {
    for(let i = 0; i < shared.finalNumber - 9; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page93") {
    for(let i = 0; i < shared.finalNumber - 9; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page94") {
    for(let i = 0; i < shared.finalNumber - 11; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page95") {
    for(let i = 0; i < shared.finalNumber - 13; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page96") {
    for(let i = 0; i < shared.finalNumber - 15; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page97") {
    for(let i = 0; i < shared.finalNumber - 17; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page98") {
    for(let i = 0; i < shared.finalNumber - 19; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page99") {
    for(let i = 0; i < shared.finalNumber - 21; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page100") {
    for(let i = 0; i < shared.finalNumber - 23; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page101") {
    for(let i = 0; i < shared.finalNumber - 25; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page102") {
    for(let i = 0; i < shared.finalNumber - 27; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page103") {
    for(let i = 0; i < shared.finalNumber - 29; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page104") {
    for(let i = 0; i < shared.finalNumber - 31; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page105") {
    for(let i = 0; i < shared.finalNumber - 33; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page106") {
    for(let i = 0; i < shared.finalNumber - 35; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page107") {
    for(let i = 0; i < shared.finalNumber - 37; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page108") {
    for(let i = 0; i < shared.finalNumber - 39; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page109") {
    for(let i = 0; i < shared.finalNumber - 41; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page110") {
    for(let i = 0; i < shared.finalNumber - 43; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page111") {
    for(let i = 0; i < shared.finalNumber - 45; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page112") {
    for(let i = 0; i < shared.finalNumber - 47; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page113") {
    for(let i = 0; i < shared.finalNumber - 49; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page114") {
    for(let i = 0; i < shared.finalNumber - 52; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page115") {
    for(let i = 0; i < shared.finalNumber - 54; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page116") {
    for(let i = 0; i < shared.finalNumber - 56; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page117") {
    for(let i = 0; i < shared.finalNumber - 58; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page118") {
    for(let i = 0; i < shared.finalNumber - 60; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page119") {
    for(let i = 0; i < shared.finalNumber - 62; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page120") {
    for(let i = 0; i < shared.finalNumber - 64; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page121") {
    for(let i = 0; i < shared.finalNumber - 66; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page122") {
    for(let i = 0; i < shared.finalNumber - 13; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page123") {
    for(let i = 0; i < shared.finalNumber - 15; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page124") {
    for(let i = 0; i < shared.finalNumber - 17; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page125") {
    for(let i = 0; i < shared.finalNumber - 19; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page126") {
    for(let i = 0; i < shared.finalNumber - 21; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page127") {
    for(let i = 0; i < shared.finalNumber - 23; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page128") {
    for(let i = 0; i < shared.finalNumber - 25; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page129") {
    for(let i = 0; i < shared.finalNumber - 27; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page130") {
    for(let i = 0; i < shared.finalNumber - 29; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page131") {
    for(let i = 0; i < shared.finalNumber - 31; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page132") {
    for(let i = 0; i < shared.finalNumber - 33; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page133") {
    for(let i = 0; i < shared.finalNumber - 35; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page134") {
    for(let i = 0; i < shared.finalNumber - 37; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page135") {
    for(let i = 0; i < shared.finalNumber - 39; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page136") {
    for(let i = 0; i < shared.finalNumber - 41; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page137") {
    for(let i = 0; i < shared.finalNumber - 43; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page138") {
    for(let i = 0; i < shared.finalNumber - 45; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page139") {
    for(let i = 0; i < shared.finalNumber - 47; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page140") {
    for(let i = 0; i < shared.finalNumber - 49; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page141") {
    for(let i = 0; i < shared.finalNumber - 51; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page142") {
    for(let i = 0; i < shared.finalNumber - 53; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page143") {
    for(let i = 0; i < shared.finalNumber - 55; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page144") {
    for(let i = 0; i < shared.finalNumber - 57; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page145") {
    for(let i = 0; i < shared.finalNumber - 59; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page146") {
    for(let i = 0; i < shared.finalNumber - 61; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page147") {
    for(let i = 0; i < shared.finalNumber - 63; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page148") {
    for(let i = 0; i < shared.finalNumber - 65; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page149") {
    for(let i = 0; i < shared.finalNumber - 67; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page150") {
    for(let i = 0; i < shared.finalNumber - 69; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page151") {
    for(let i = 0; i < shared.finalNumber - 71; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page152") {
    for(let i = 0; i < shared.finalNumber - 73; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page153") {
    for(let i = 0; i < shared.finalNumber - 75; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page154") {
    for(let i = 0; i < shared.finalNumber - 77; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page155") {
    for(let i = 0; i < shared.finalNumber - 79; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page156") {
    for(let i = 0; i < shared.finalNumber - 81; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page157") {
    for(let i = 0; i < shared.finalNumber - 83; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page158") {
    for(let i = 0; i < shared.finalNumber - 85; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page159") {
    for(let i = 0; i < shared.finalNumber - 87; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page165") {
    for(let i = 0; i < shared.finalNumber; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page166") {
    for(let i = 0; i < shared.finalNumber - 2; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  else if(page == "text_page167") {
    for(let i = 0; i < shared.finalNumber - 4; i ++ ) {
      text(textName[i], 50, text_start + i * 20, 500, 60);
    }
  }
  else if(page == "text_page168") {
    for(let i = 0; i < shared.finalNumber; i ++ ) {
      text(textName[i], 50, text_start + i * text_dist, 500, 60);
    }
  }
  pop();
}
