function finalStage() {
  
  if(shared.finalNumber >= 0 && shared.finalNumber <= 1) {
    prologueScreen87();
    page = "text_page87";
  }
  else if(shared.finalNumber > 1 && shared.finalNumber <= 3) {
    prologueScreen88();
    page = "text_page88";
  }
  else if(shared.finalNumber > 3 && shared.finalNumber <= 5) {
    prologueScreen89();
    page = "text_page89";
  }
  else if(shared.finalNumber > 5 && shared.finalNumber <= 7) {
    prologueScreen90();
    page = "text_page90";
  }
  else if(shared.finalNumber > 7 && shared.finalNumber <= 9) {
    prologueScreen91();
    page = "text_page91";
  }
  else if(shared.finalNumber > 9 && shared.finalNumber <= 11) {
    if(shared.answer5 == "A") {
      prologueScreen92();
      page = "text_page92";
    }
    else if(shared.answer5 == "B") {
      prologueScreen93();
      page = "text_page93";
    }
  }
  else if(shared.finalNumber > 11 && shared.finalNumber <= 13) {
    prologueScreen94();
    page = "text_page94";
  }
  
  if(shared.answer5 == "B") {
    if(shared.finalNumber > 13 && shared.finalNumber <= 15) {
      prologueScreen122();
      page = "text_page122";
    }
    else if(shared.finalNumber > 15 && shared.finalNumber <= 17) {
      prologueScreen123();
      page = "text_page123";
    }
    else if(shared.finalNumber > 17 && shared.finalNumber <= 19) {
      prologueScreen124();
      page = "text_page124";
    }
    else if(shared.finalNumber > 19 && shared.finalNumber <= 21) {
      prologueScreen125();
      page = "text_page125";
    }
    else if(shared.finalNumber > 21 && shared.finalNumber <= 23) {
      prologueScreen126();
      page = "text_page126";
    }
    else if(shared.finalNumber > 23 && shared.finalNumber <= 25) {
      prologueScreen127();
      page = "text_page127";
    }
    else if(shared.finalNumber > 25 && shared.finalNumber <= 27) {
      prologueScreen128();
      page = "text_page128";
    }
    else if(shared.finalNumber > 27 && shared.finalNumber <= 29) {
      prologueScreen129();
      page = "text_page129";
    }
    else if(shared.finalNumber > 29 && shared.finalNumber <= 31) {
      prologueScreen130();
      page = "text_page130";
    }
    else if(shared.finalNumber > 31 && shared.finalNumber <= 33) {
      prologueScreen131();
      page = "text_page131";
    }
    else if(shared.finalNumber > 33 && shared.finalNumber <= 35) {
      prologueScreen132();
      page = "text_page132";
    }
    else if(shared.finalNumber > 35 && shared.finalNumber <= 37) {
      prologueScreen133();
      page = "text_page133";
    }
    else if(shared.finalNumber > 37 && shared.finalNumber <= 39) {
      prologueScreen134();
      page = "text_page134";
    }
    else if(shared.finalNumber > 39 && shared.finalNumber <= 41) {
      prologueScreen135();
      page = "text_page135";
    }
    else if(shared.finalNumber > 41 && shared.finalNumber <= 43) {
      prologueScreen136();
      page = "text_page136";
    }
    else if(shared.finalNumber > 43 && shared.finalNumber <= 45) {
      prologueScreen137();
      page = "text_page137";
    }
    else if(shared.finalNumber > 45 && shared.finalNumber <= 47) {
      prologueScreen138();
      page = "text_page138";
    }
    else if(shared.finalNumber > 47 && shared.finalNumber <= 49) {
      prologueScreen139();
      page = "text_page139";
    }
    else if(shared.finalNumber > 49 && shared.finalNumber <= 51) {
      prologueScreen140();
      page = "text_page140";
    }
    else if(shared.finalNumber > 51 && shared.finalNumber <= 53) {
      prologueScreen141();
      page = "text_page141";
    }
    else if(shared.finalNumber > 53 && shared.finalNumber <= 55) {
      prologueScreen142();
      page = "text_page142";
    }
    else if(shared.finalNumber > 55 && shared.finalNumber <= 57) {
      prologueScreen143();
      page = "text_page143";
    }
    else if(shared.finalNumber > 57 && shared.finalNumber <= 59) {
      prologueScreen144();
      page = "text_page144";
    }
    else if(shared.finalNumber > 59 && shared.finalNumber <= 61) {
      prologueScreen145();
      page = "text_page145";
    }
    else if(shared.finalNumber > 61 && shared.finalNumber <= 63) {
      prologueScreen146();
      page = "text_page146";
    }
    else if(shared.finalNumber > 63 && shared.finalNumber <= 65) {
      prologueScreen147();
      page = "text_page147";
    }
    else if(shared.finalNumber > 65 && shared.finalNumber <= 67) {
      prologueScreen148();
      page = "text_page148";
    }
    else if(shared.finalNumber > 67 && shared.finalNumber <= 69) {
      prologueScreen149();
      page = "text_page149";
    }
    else if(shared.finalNumber > 69 && shared.finalNumber <= 71) {
      prologueScreen150();
      page = "text_page150";
    }
    else if(shared.finalNumber > 71 && shared.finalNumber <= 73) {
      prologueScreen151();
      page = "text_page151";
    }
    else if(shared.finalNumber > 73 && shared.finalNumber <= 75) {
      prologueScreen152();
      page = "text_page152";
    }
    else if(shared.finalNumber > 75 && shared.finalNumber <= 77) {
      prologueScreen153();
      page = "text_page153";
    }
    else if(shared.finalNumber > 77 && shared.finalNumber <= 79) {
      prologueScreen154();
      page = "text_page154";
    }
    else if(shared.finalNumber > 79 && shared.finalNumber <= 81) {
      prologueScreen155();
      page = "text_page155";
    }
    else if(shared.finalNumber > 81 && shared.finalNumber <= 83) {
      prologueScreen156();
      page = "text_page156";
    }
    else if(shared.finalNumber > 83 && shared.finalNumber <= 85) {
      prologueScreen157();
      page = "text_page157";
    }
    else if(shared.finalNumber > 85 && shared.finalNumber <= 87) {
      prologueScreen158();
      page = "text_page158";
    }
    else if(shared.finalNumber > 87 && shared.finalNumber <= 89) {
      prologueScreen159();
      page = "text_page159";
    }
    else if(shared.finalNumber > 89) {
      end();
    }
  }
  
  else if(shared.answer5 == "A") {
    if(shared.finalNumber > 13 && shared.finalNumber <= 15) {
      prologueScreen95();
      page = "text_page95";
    }
    if(shared.finalNumber > 15 && shared.finalNumber <= 17) {
      prologueScreen96();
      page = "text_page96";
    }
    if(shared.finalNumber > 17 && shared.finalNumber <= 19) {
      prologueScreen97();
      page = "text_page97";
    }
    if(shared.finalNumber > 19 && shared.finalNumber <= 21) {
      prologueScreen98();
      page = "text_page98";
    }
    if(shared.finalNumber > 21 && shared.finalNumber <= 23) {
      prologueScreen99();
      page = "text_page99";
    }
    if(shared.finalNumber > 23 && shared.finalNumber <= 25) {
      prologueScreen100();
      page = "text_page100";
    }
    if(shared.finalNumber > 25 && shared.finalNumber <= 27) {
      prologueScreen101();
      page = "text_page101";
    }
    if(shared.finalNumber > 27 && shared.finalNumber <= 29) {
      prologueScreen102();
      page = "text_page102";
    }
    if(shared.finalNumber > 29 && shared.finalNumber <= 31) {
      prologueScreen103();
      page = "text_page103";
    }
    if(shared.finalNumber > 31 && shared.finalNumber <= 33) {
      prologueScreen104();
      page = "text_page104";
    }
    if(shared.finalNumber > 33 && shared.finalNumber <= 35) {
      prologueScreen105();
      page = "text_page105";
    }
    if(shared.finalNumber > 35 && shared.finalNumber <= 37) {
      prologueScreen106();
      page = "text_page106";
    }
    if(shared.finalNumber > 37 && shared.finalNumber <= 39) {
      prologueScreen107();
      page = "text_page107";
    }
    if(shared.finalNumber > 39 && shared.finalNumber <= 41) {
      prologueScreen108();
      page = "text_page108";
    }
    if(shared.finalNumber > 41 && shared.finalNumber <= 43) {
      prologueScreen109();
      page = "text_page109";
    }
    if(shared.finalNumber > 43 && shared.finalNumber <= 45) {
      prologueScreen110();
      page = "text_page110";
    }
    if(shared.finalNumber > 45 && shared.finalNumber <= 47) {
      prologueScreen111();
      page = "text_page111";
    }
    if(shared.finalNumber > 47 && shared.finalNumber <= 49) {
      prologueScreen112();
      page = "text_page112";
    }
    if(shared.finalNumber > 49 && shared.finalNumber <= 51) {
      prologueScreen113();
      page = "text_page113";
    }
    if(shared.finalNumber == 52) {
      choiceScreen10_1();
      page = "choice_page10_1";
    }
    if(shared.finalNumber > 52 && shared.finalNumber <= 54) {
      prologueScreen114();
      page = "text_page114";
    }
    if(shared.finalNumber > 54 && shared.finalNumber <= 56) {
      prologueScreen115();
      page = "text_page115";
    }
    if(shared.finalNumber > 56 && shared.finalNumber <= 58) {
      prologueScreen116();
      page = "text_page116";
    }
    if(shared.finalNumber > 58 && shared.finalNumber <= 60) {
      prologueScreen117();
      page = "text_page117";
    }
    if(shared.finalNumber > 60 && shared.finalNumber <= 62) {
      prologueScreen118();
      page = "text_page118";
    }
    if(shared.finalNumber > 62 && shared.finalNumber <= 64) {
      prologueScreen119();
      page = "text_page119";
    }
    if(shared.finalNumber > 64 && shared.finalNumber <= 66) {
      prologueScreen120();
      page = "text_page120";
    }
    if(shared.finalNumber > 66 && shared.finalNumber <= 68) {
      prologueScreen121();
      page = "text_page121";
    }
    if(shared.finalNumber > 68) {
      end();
    }
  }
}